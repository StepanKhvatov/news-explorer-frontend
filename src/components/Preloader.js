import React from 'react';
import '../vendor/preloader/preloader.scss';

const Proloader = () => (
    <section className="section_results">
        <i className="preloader"></i>
        <p className="preloader__subtitle">Идёт поиск новостей...</p>
    </section>
);

export default Proloader;
